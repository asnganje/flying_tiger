[project]
name = "flying-tiger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "asnganje", email = "abd.nganje@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
flying-tiger = "flying_tiger:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
